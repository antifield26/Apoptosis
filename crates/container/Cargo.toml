[package]
name = "mc-container"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
mc-core = { path = "../core" }
# Recipe data loading (P07-09): the furnace table is built from the real
# smelting recipes the pack ships rather than a hand-written list.
mc-data = { path = "../data" }
mc-entity = { path = "../entity" }
mc-registry = { path = "../registry" }
tracing.workspace = true

[dev-dependencies]
mc-test-support = { path = "../test-support" }
